"""Веб-хостинг презентаций, опубликованных из плагина Figma `figma-plugin/`.

Отдельный от `jobbot` сервис — принимает слайды (текст/фигуры/картинки,
тот же формат, что и в pptx-экспорте плагина), отдаёт их по shareable-ссылке
как HTML-слайд-шоу и считает простую аналитику просмотров.

  uvicorn pitchdeck_server.app:app --host 0.0.0.0 --port 8000   # или python -m pitchdeck_server
"""
